services:
  api:
    build: .
    ports: ["8000:8000"]
    env_file: .env
    depends_on: [db]
  db:
    image: postgres:16
    environment:
      POSTGRES_DB: rural
      POSTGRES_USER: rural
      POSTGRES_PASSWORD: rural
    volumes:
      - pgdata:/var/lib/postgresql/data
volumes:
  pgdata: